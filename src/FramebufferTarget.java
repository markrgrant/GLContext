
enum FramebufferTarget {
    GL_FRAMEBUFFER, // set buffer to both the read and write targets
    GL_READ_FRAMEBUFFER, // set buffer to a read only target
    GL_DRAW_FRAMEBUFFER  // set buffer to  write only target
}
